# NVIDIA Nemotron Voice Agent Requirements
# Tested on: RTX 4060 Ti (16GB) + Titan V (12GB) + 64GB RAM
# CUDA Toolkit: 11.8
# OS: Ubuntu 22.04
# Python: 3.11

# ==============================================================================
# PyTorch with CUDA 11.8 (install first, separately)
# ==============================================================================
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Verify with:
# python -c "import torch; print(f'PyTorch: {torch.__version__}, CUDA: {torch.version.cuda}')"
# Expected: PyTorch: 2.7.1+cu118, CUDA: 11.8

# ==============================================================================
# NVIDIA NeMo for ASR
# ==============================================================================
Cython
nemo_toolkit[asr]

# ==============================================================================
# Transformers + Quantization (for Nemotron Nano 9B)
# ==============================================================================
transformers>=4.53.0
accelerate>=0.30.0
sentencepiece
protobuf
bitsandbytes>=0.49.0

# Mamba-SSM for Nemotron's hybrid architecture
# Install with: pip install causal-conv1d && pip install mamba-ssm --no-build-isolation
causal-conv1d>=1.5.0
# mamba-ssm>=2.2.6  # Install separately with --no-build-isolation

# ==============================================================================
# Audio Processing
# ==============================================================================
# System dependency: sudo apt install portaudio19-dev
pyaudio>=0.2.14
soundfile>=0.12.0
librosa>=0.10.0
numpy>=1.26.0

# ==============================================================================
# TTS (Silero - downloaded via torch.hub)
# ==============================================================================
# No pip install needed - loaded via:
# torch.hub.load('snakers4/silero-models', model='silero_tts', language='en', speaker='v3_en')

# ==============================================================================
# HuggingFace Tools
# ==============================================================================
huggingface_hub[cli]>=0.36.0

# ==============================================================================
# Web/API Server (optional - for serving models)
# ==============================================================================
websockets>=12.0
aiohttp>=3.9.0
fastapi>=0.115.0
uvicorn>=0.32.0

# ==============================================================================
# Utilities
# ==============================================================================
einops>=0.8.0
packaging>=24.0
pydantic>=2.0.0
tqdm>=4.67.0

# ==============================================================================
# NOT USED (architecture not yet supported)
# ==============================================================================
# llama-cpp-python  # nemotron_h architecture not supported yet
# vllm              # Not needed when using transformers + bitsandbytes
# pipecat-ai        # Optional voice agent framework
